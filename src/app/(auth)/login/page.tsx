import { LoginForm } from "@/features/auth/components/login-form";

export const metadata = {
  title: "Login - Crest",
  description: "Login to your Crest account",
};

export default function LoginPage() {
  return <LoginForm />;
}
